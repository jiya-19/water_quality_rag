services:
  - type: web
    name: water-quality-rag
    runtime: python

    buildCommand: |
      pip install -r requirements.txt
      python scripts/build_index.py

    startCommand: |
      uvicorn app.api.main:app --host 0.0.0.0 --port $PORT

    plan: free